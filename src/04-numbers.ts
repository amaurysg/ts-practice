(() => {
  let productPrice = 1500;
  productPrice = 1400;
  console.log(productPrice);

  let otherSide = productPrice + 'k';
  console.log('otherSide', otherSide);


})();

let valueOther:number = 93;
if (valueOther > 20) {
  console.log('greater');
}
